name: First Workflow
on:
  workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: my first step
      run: echo "Hello World"
    - name: my second step
      run: echo "Done - Bye"
